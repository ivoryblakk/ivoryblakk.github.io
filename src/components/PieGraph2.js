import  React from 'react';
import DonutChart from 'react-d3-donut';

export default class CodeWarsDoughnutGraph extends React.Components {

    render() {
        return (
            <div>
                asdfasdfadsf
            </div>
        );
    }
}
/*  <DonutChart
                    innerRadius={50}
                    outerRadius={70}
                    transition={false}
                    svgClass="CodeWwarsPie"
                    pieClass="pie"
                    displayTooltip={true}
                    strokeWidth={5}
                    data={this.state.codewarsUserData} />
*/
//export default CodeWarsDoughnutGraph;
